es_host: elasticsearch
es_port: 9200

# Indices named elastalert2_alerts* — matches check-health.sh query
writeback_index: elastalert2_alerts

run_every:
  seconds: 5

buffer_time:
  days: 180

rules_folder: /opt/elastalert2/rules

alert_time_limit:
  minutes: 1
